(Generated by ChatGPT)

<b>Overview</b>

This repository is a PHP‑based web application used to maintain athletics records for multiple sports.
The entry point is index.php, which includes each sport module and renders a menu of sports for users to select. Based on query parameters it calls the appropriate function (e.g., fbRecords, mbbRecords) to display records for that sport:
```
include("database.php");        // Main database functions
include("mbb.php");             // Men’s basketball
...
<a href="index.php?SPORT=fb"><button>Football</button></a>
...
if ($requestedSport != NULL){
    switch($requestedSport){
        case "fb":
            echo "Football<br>";
            fbRecords();
            break;
        ...
    }
}
```
<b>Database Interaction</b>

General database functions reside in database.php. On startup it loads connection info from config.ini and establishes a mysqli connection. Utility functions such as readCurrentDatabase handle fetching records for different levels (game, season, career, etc.):
```
$config = parse_ini_file('config.ini');
$conn = mysqli_connect($config['db_path'], $config['db_user'],
                       $config['db_pass'], $config['db_name'], $config['db_port']);

function readCurrentDatabase($db_name, $table, $stat, $level, $sql_override = NULL)
{
    ...
    if ($level == "TMG") $sql = "SELECT * FROM `$table` as b LEFT OUTER JOIN ...";
    else if ($level == "GAM") $sql = "SELECT * FROM `$table` as b LEFT OUTER JOIN ...";
    else if ($level == "CAR") $sql = "SELECT Name,SUM(Value) AS Value ...";
    ...
    $result = mysqli_query($conn, $sql);
    if (mysqli_num_rows($result) > 0) {
        echo "<script>document.getElementById('Success').textContent='Query Successful.'</script>";
        ...
        return ($result);
    }
    ...
}
```
The file also contains numerous helper functions for printing tables, editing records, updating athletes, and interacting with game data.

In general, each sport requires four tables: <br>
athletes - Stores each athlete's information for display (name, season active, IsActive (Boolean)<br>
boxscores - Stores all stats for the games using uploaded XML files. New records are auto-calculated using these entries<br>
oldrecords - Records that are entered and stored verbatem then regurgitated in the appropriate database call.<br>
games - Stores all game information from each XML file uploaded. It includes identifiers including RPI code of opponent, season the game was played (extrapolated from the date), final score, final result.<br>
<br>
There is one general purpose database - NCAA_RPI: Stores opponent information according to the RPI code of the opponent. For NCAA, this is standardized and set according to NCAA. Non-NCAA application will require a custom opponent code list.<br>

<b>Sport Modules</b>

Each sport has its own PHP module (e.g., fb.php for football, wbb.php for women’s basketball, vb.php for volleyball). These modules interpret query parameters for a stat and level, fetch records via readCurrentDatabase / readOldDatabase, and present results. They also define upload functions used when importing XML boxscore data. As an example, vb.php examines the requested stat and chooses custom SQL queries when needed:
```
function vbRecords()
{
    ...
    $currentRecords = readCurrentDatabase($db_name,$table,$requestedStat,$_GET['LEVEL']);
    $oldRecords = readOldDatabase($db_name,"vb_oldrecords",$requestedStat,$_GET['LEVEL']);
    if ($requestedLevel == "GAM"){
        if ($requestedStat == "attack_k"){
            $oldRecords = readOldDatabase($db_name,"vb_oldrecords",$requestedStat,$_GET['LEVEL'],"DESC","SELECT * FROM `vb_oldrecords` WHERE `Stat`='attack_k3' ...");
            printCurrentRecordsDatabase($currentRecords, $oldRecords, "vb", FALSE);
        }
        ...
    }
}
```
<b>Configuration</b>

Thresholds for season and career milestones are stored in .ini files per sport. For instance, vb.ini defines milestone values for volleyball:
```
[Volleyball Season Milestones]
percent = 75; Percent Reached Before Notification
sea_attack_k = 300; Kills
...
[Volleyball Career Milestones]
car_percent = 90; Percent Reached Before Notification
car_attack_k[] = 1000; Kills
car_attack_k[] = 1500; Kills
...
```
config.ini stores database connection credentials.

<b>JavaScript Helpers</b>

Small JavaScript files support the UI. makeselection.js updates the page when a user selects a sport, level, or stat:
```
function madeRecordsSelection() {
    var e = document.getElementById("indexLevel");
    var sportPosition = value.indexOf("_");
    var sport = value.substring(0,sportPosition);
    var level = value.substring(sportPosition+1);
    var newLocation = "index.php?SPORT="+sport+"&LEVEL="+level;
    window.location.replace(newLocation);
}
...
levelSelector.addEventListener("change", madeRecordsSelection, false);
statSelector.addEventListener("change", madeStatSelection, false);
```
<b>Admin & Upload</b>

record_administration.php is an administration interface that includes buttons for each sport, file upload links, and functions such as editfbRecords or editmbbRecords for editing existing entries.
```
<h1 align="center">Welcome to the WVSU Athletics Records Database Administration</h1>
<a href="upload_file.php"><button>Upload File</button></a>
...
if ($requestedSport != NULL){
    switch($requestedSport){
        case "mbb":
            include "mbb.php";
            echo "Men's Basketball<br>";
            editmbbRecords();
            break;
        ...
```
Uploads are processed by upload.php and write_database_file.php, which read the uploaded XML/StatCrew files and insert new records into the database.

<b>Query Helpers</b>

queries.php contains a simple class (SoccerQueryManager) used by the soccer module to map level/stat pairs to SQL statements:
```
class SoccerQueryManager {
    public static function get($level, $stat) {
        $config = self::getQueryConfig();
        return isset($config[$level][$stat]) ? $config[$level][$stat] : null;
    }
    private static function getQueryConfig() {
        return [
            'GAM' => [
                'points' => "SELECT * FROM `wsoc_boxscores` as b LEFT JOIN `wsoc_games` AS g ...",
                ...
            ],
            ...
        ];
    }
}
```
<hr>
Next Steps / Areas to Explore

    Database Schema – Review the database structure to understand how athlete, game, and record tables are organized. The PHP code expects tables named <sport>_boxscores, <sport>_games, <sport>_athletes, etc.

    Upload Scripts – Look at the upload functions within each sport module and write_database_file.php to see how XML/StatCrew data is parsed and inserted.

    Milestone Alerts – Check how the milestone .ini files are used. These thresholds likely generate notifications when a player is approaching a record.

    Front‑end Enhancements – The UI is mostly plain PHP/HTML with minimal JavaScript. There’s room for modernizing the front end or adding validation.

    Security Considerations – Current scripts directly print SQL queries for debugging and rely on raw POST/GET parameters. Input validation and prepared statements would improve security.

Overall, the application is a collection of procedural PHP scripts that manage record queries, editing, and importing for multiple sports. Understanding database.php and one of the sport modules (e.g., fb.php) will give you a good foundation to extend or refactor the system.
